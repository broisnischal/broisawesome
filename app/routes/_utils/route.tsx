/**
 * Pathless layout route for utility/resource routes.
 * This route doesn't add any path segments to the URL.
 * Child routes will appear at their configured paths without the _utils prefix.
 */
export default function UtilsLayout() {
  return null;
}
